name: 'Projekt einrichten'
description: 'Code auschecken, Node.js einrichten und Abhängigkeiten installieren'

runs:
  using: 'composite'
  steps:
    - name: Code auschecken
      uses: actions/checkout@v4

    - name: Node.js einrichten
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Abhängigkeiten installieren
      run: npm ci
      shell: bash
